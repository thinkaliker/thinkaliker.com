language: ruby

branches:
  only:
    - master

rvm:
  - 2.4.1

cache: bundler

addons:
  apt:
    packages:
    - libcurl4-openssl-dev

before_script:
  - chmod +x ./script/cibuild

script: ./script/cibuild
exclude: [vendor]
sudo: false